<script type="text/x-template" id="modal-template">
    <div class="modal fade" v-bind:id="tinfo.id" role="basic" aria-hidden="true" v-bind:class="tinfo.class">
        <div class="modal-dialog" v-bind:class="tinfo.modalclass">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
                    <h4 class="modal-title">{{tinfo.title}}</h4>
                </div>
                <div class="modal-body">
                    <slot></slot>
                </div>
                <div class="modal-footer">
                    <slot name="bottom"></slot>
                    <button type="button" class="btn blue btn-outline" data-dismiss="modal">关闭</button>
                </div>
            </div>
        </div>
    </div>
</script>